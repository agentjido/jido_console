version: 1
suite:
  id: offline_fixture
  agents:
    - key: offline
      file: agent.yml
  scenarios:
    - scenario.yml
  models:
    - key: declared
      source: agent
  matrix:
    repeats: 1
  run:
    jobs: 1
