# Scenario Rules

- Change only `lib/rate_limiter.ex`.
- Do not change tests or project configuration.
- Preserve the public `RateLimiter.allow?/4` function.
- Run the `mix-test` verification helper after the change.
- Review the final Git diff before you report completion.
